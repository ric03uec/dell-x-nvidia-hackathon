"""Ingestion API (component 2).

Scaffold only — the schema, adapters, and the real API surface land in
epic 2 (see docs/modular-implementation-plan.md). This package currently
exposes nothing but a FastAPI app with a `/health` endpoint: no persistence
of any kind, SQLite included, lives here yet.
"""

import './App.css'

/**
 * Placeholder landing page for the exfiltration-protection dashboard.
 *
 * This is the drop-in point for the prototype TypeScript/React app: replace
 * this component (and wire in routing/data-fetching as needed) without
 * touching the surrounding toolchain (package.json, vite.config.ts,
 * justfile).
 */
function App() {
  return (
    <main id="placeholder">
      <h1>Dashboard</h1>
      <p>
        Scaffold only — drop the prototype app into <code>src/App.tsx</code>.
      </p>
    </main>
  )
}

export default App
